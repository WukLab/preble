# Test Loading multiple models then clearing the memory. The resulting memory should be empty on both gpus

# Test forwarding logs for each server logs for each model

# Test Loading a model and sending a generate request

# Test Loading a model and sending a forward request
