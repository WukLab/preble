# TODO add benchmark
# Verify that there is a minimal overhead while scheduling when using the info from the metrics server
# %timeit r = requests.post(metrics_url, json={"prompt": react_workload})


